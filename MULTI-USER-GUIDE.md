# מדריך הקמת רב-משתמשים — קליניקת ארגמן

**עדכון:** 18 במאי 2026
**עבור:** גל ממן + צוות

---

## למה זה חשוב?

עד היום: סיסמה אחת על כל מכשיר, נתונים לא מסונכרנים בין מכשירים בזמן אמת, ואין הפרדה בין "בעל המאגר" (גל — רואה הכל) לבין "מפתח" (רועי — לא רואה מידע רפואי מוגן).

מהיום: **מערכת רב-משתמשים** עם:
- ☑️ סיסמה אחת לכל מכשיר (Supabase Auth)
- ☑️ סנכרון נתונים בזמן אמת בין מכשירים
- ☑️ הפרדת פרטיות רפואית: גל רואה הכל, מפתח לא רואה מטופלים/לידים/רשומות
- ☑️ הרחבה עתידית לצוות נוסף

---

## שלב 1: גל יוצר משתמשי Auth ב-Supabase

### 1.1 כניסה לDashboard

1. גש ל-[supabase.com/dashboard](https://supabase.com/dashboard)
2. התחבר עם החשבון שלך
3. בחר את הפרויקט `rrvjiudtgooyxpbboary`

### 1.2 יצירת משתמש Owner (גל)

1. בתפריט הצדדי → **Authentication** → **Users**
2. לחץ **Add user** → **Create new user**
3. **Email:** `argamanclinic@gmail.com`
4. **Password:** סיסמה חזקה משלך (מינ' 12 תווים, אותיות גדולות+קטנות+מספרים+סמלים)
5. ✓ סמן **Auto Confirm User** (אישור אוטומטי)
6. לחץ **Create User**
7. **שמור את ה-UID** שמופיע — תצטרך אותו

### 1.3 יצירת משתמש Developer (רועי)

חזור על אותו תהליך:
- **Email:** רועי יספק לך את האימייל שלו
- **Password:** רועי ייצור משלו (שלח לו בנפרד)
- העתק את ה-UID שלו

---

## שלב 2: הרצת SQL ב-Supabase

### 2.1 פתיחת SQL Editor

ב-Dashboard → **SQL Editor** (אייקון בצורת הסעיף `<>`)

### 2.2 הרצת auth-migration.sql

1. פתח את הקובץ `auth-migration.sql` (אצלך בפרויקט)
2. העתק את כל התוכן
3. הדבק ב-SQL Editor
4. **לפני שלוחץ Run** — לך לסוף הקובץ
5. בחלק שמופיע `-- Example (replace UUIDs!)` — הסר את `--` מתחילת כל שורה
6. החלף את ה-UUIDs בערכים האמיתיים שקיבלת בשלב 1 (UID של גל ו-UID של רועי)
7. בדוק שהאימיילים נכונים
8. לחץ **Run** (ירוק למטה)
9. אמור להופיע "Success. No rows returned"

### 2.3 הרצת rls-policies.sql

1. פתח את הקובץ `rls-policies.sql`
2. העתק את כל התוכן ל-SQL Editor
3. לחץ **Run**
4. אמור להופיע "Success"

---

## שלב 3: וידוא שזה עובד

### 3.1 בדיקה מהיר ב-Supabase

ב-SQL Editor, הרץ:
```sql
SELECT user_id, email, role, display_name, can_see_phi
FROM argaman_users;
```

אמור להחזיר 2 שורות:
- גל (`owner`, `can_see_phi=true`)
- רועי (`developer`, `can_see_phi=false`)

### 3.2 בדיקה ב-CRM (אחרי deploy)

1. גל נכנס ל-[argamanclinic.com/admin.html](https://argamanclinic.com/admin.html)
2. במסך login: **Email** + **Password** (חדש — לא הסיסמה הישנה)
3. אמור להיכנס בהצלחה ולראות הכל
4. רועי עושה אותו דבר מהמחשב שלו — נכנס ורואה רק תוכן/SEO/הגדרות (לא לקוחות)

---

## שלב 4: שינוי סיסמה (גם בעתיד)

**גל יכול לשנות סיסמה מעכשיו דרך:**

ב-CRM → Sidebar → 🔒 אבטחה → "שינוי סיסמה"
(נחבר את זה ל-Supabase Auth — `supa.auth.updateUser({password})`)

או דרך Supabase Dashboard → Authentication → Users → ⋮ → Reset Password

**הסיסמה החדשה תעבוד בכל המכשירים מיד.**

---

## שלב 5: הוספת משתמש נוסף בעתיד (Staff)

כשגל רוצה להוסיף עוזר/ת:

### 5.1 ב-Supabase Dashboard
1. Authentication → Users → Add User
2. אימייל + סיסמה
3. העתק UID

### 5.2 ב-SQL Editor
```sql
INSERT INTO argaman_users (user_id, email, role, display_name, can_see_phi)
VALUES (
  'UID-של-העוזרת',
  'email@example.com',
  'staff',
  'שם מלא',
  TRUE  -- staff רואה PHI (כי עובדת עם הלקוחות)
);
```

### 5.3 הגדרת הרשאות
ה-RLS הקיים תומך ב-3 roles:
- `owner` — גל — רואה הכל, יכול לערוך הכל
- `staff` — עוזר/ת — רואה הכל, יכול לערוך הכל מלבד הגדרות מערכת
- `developer` — רועי — רואה הכל **חוץ ממידע רפואי**

לרצונכם להוסיף role עם הרשאות מותאמות אישית — פנו אליי לעדכון ה-SQL.

---

## שאלות נפוצות

### ❓ מה קורה לסיסמה הישנה (argaman2025)?

**מבוטלת.** מהדפלוי הבא, הכניסה דורשת:
- אימייל (מתוך ה-2 שיצרת ב-Supabase)
- סיסמה חדשה שיצרת ב-Supabase

הסיסמה הישנה לא תעבוד יותר.

### ❓ מה אם אני שוכח/ת את הסיסמה?

ב-Supabase Dashboard → Authentication → Users → ⋮ → **Reset Password** — שולח לך מייל לאיפוס.

### ❓ מה קורה לקוד השחזור (Recovery Code) הישן?

הוא **לא תקף יותר** במערכת החדשה — Supabase Auth מנהל את האימות.
אם איבדת DEK (נתונים מוצפנים), קוד שחזור עדיין יכול לעזור — אבל זה תרחיש נדיר.

### ❓ האם נתונים שאני יוצר אצלי יראו אצל גל בזמן אמת?

**כן** — אחרי שלב 2 ב-Deploy. תוך 1-2 שניות.

### ❓ האם רועי יכול לראות שמות מטופלים?

**לא.** רועי רואה:
- ✓ הגדרות מערכת
- ✓ תוכן האתר (מאמרים, FAQ, סדנאות)
- ✓ SEO + שיווק
- ✓ דוחות אגרגטיביים (מספרים בלבד)
- ✗ **לא** רואה: לקוחות, לידים, פגישות, רשומות SOAP, מדדי outcome, וכו'

זה אכוף ברמת **Supabase RLS** — בלתי אפשרי לעקוף גם דרך הקוד.

---

## בטחון

המערכת עומדת בתאימות:
- **תיקון 13** לחוק הגנת הפרטיות הישראלי (חל 14.8.2025)
- **הפרדת תפקידים** (Separation of Duties) — אחד מעקרונות אבטחת מידע
- **Least Privilege** — כל משתמש רואה רק מה שצריך

---

## תמיכה

בעיות? פנה ל-roeimaman21@gmail.com

לפני שאתה קורא — שלח:
1. צילום מסך של השגיאה
2. צילום מסך מה-Console (F12)
3. תיאור מה ניסית לעשות
